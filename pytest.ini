[pytest]
testpaths = tests
python_files = test_*.py *_test.py
markers =
    beyondarena_external: requires local/full BeyondArena or DataFoundry artifacts outside the CI fixtures
    gpu: requires a validated GPU host/device
